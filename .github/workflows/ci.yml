name: CI Example

on: [push]

jobs:
  first-job:
    runs-on: ubuntu-latest
    steps:
      - name: Print message
        run: echo "Pipeline executado com sucesso! Feito por Gabriel Falcão da Cruz"
